/**
 * @author WMXPY
 * @namespace Declare
 * @description Declare
 */

export enum PROTOCOL {

    GET = 'GET',
    POST = 'POST',
}

export enum TYPE {

    STRING = 'STRING',
    INTEGER = 'INTEGER',
    FLOAT = 'FLOAT',
    ARRAY = 'ARRAY',
    OBJECT = 'OBJECT',
}
